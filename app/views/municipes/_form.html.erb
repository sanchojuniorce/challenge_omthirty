<%= form_with(model: municipe, html: { data: { controller: "masks" } }) do |form| %>
  <%= render "/municipes/partials/municipe", form: form, required: required, disabled: disabled %>
  <%= render "/municipes/partials/endereco", form: form, required: required, readonly: readonly, disabled: disabled %>
<% end %>  
